name: release
on:
  push:
    tags: ['motrix.*@*']
permissions:
  contents: read
jobs:
  # Split build/sign design: this job runs the attacker-influenceable tag tree
  # (install lifecycle scripts, tests, esbuild) WITHOUT any secret or
  # environment binding. It can be compromised by a malicious tag tree, but
  # has nothing to steal and cannot publish anything.
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with: { version: 10 }
      - uses: actions/setup-node@v7
        with: { node-version: 24, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - name: Parse tag
        id: tag
        run: |
          json=$(node scripts/parse-tag.mjs "$GITHUB_REF_NAME")
          echo "id=$(echo "$json" | node -e 'process.stdout.write(JSON.parse(require("node:fs").readFileSync(0,"utf8")).id)')" >> "$GITHUB_OUTPUT"
          echo "version=$(echo "$json" | node -e 'process.stdout.write(JSON.parse(require("node:fs").readFileSync(0,"utf8")).version)')" >> "$GITHUB_OUTPUT"
      - name: Assert manifest version matches tag
        env:
          PLUGIN_ID: ${{ steps.tag.outputs.id }}
          PLUGIN_VERSION: ${{ steps.tag.outputs.version }}
        run: |
          mv=$(node -e 'process.stdout.write(JSON.parse(require("node:fs").readFileSync("plugins/"+process.env.PLUGIN_ID+"/motrix-plugin.json","utf8")).version)')
          [ "$mv" = "$PLUGIN_VERSION" ] || { echo "manifest $mv != tag $PLUGIN_VERSION"; exit 1; }
      - run: pnpm -r build
      - run: pnpm -r typecheck
      - run: pnpm test
      - name: Pack
        env:
          PLUGIN_ID: ${{ steps.tag.outputs.id }}
        run: node scripts/pack.mjs "$PLUGIN_ID"
      - name: Upload unsigned artifact
        uses: actions/upload-artifact@v7
        with:
          name: moext-unsigned
          path: |
            dist/artifacts/${{ steps.tag.outputs.id }}-${{ steps.tag.outputs.version }}.moext
            dist/artifacts/${{ steps.tag.outputs.id }}-${{ steps.tag.outputs.version }}.metadata.json
          if-no-files-found: error
          retention-days: 1

  # The ONLY job that can see the signing key (MOTRIX_PLUGIN_SIGNING_KEY,
  # scoped to the plugin-signing Environment; its required-reviewer rule and
  # the protected-tag ruleset for motrix.*@* were verified active on
  # 2026-07-22 — re-check with `gh ruleset list` /
  # `gh api repos/<owner>/<repo>/environments/plugin-signing`).
  #
  # It executes NO code from the tag tree: every script it runs comes from a
  # pinned checkout of main, the tag commit must already be an ancestor of
  # main (i.e. reviewed code), and it signs only the PRE-BUILT bytes handed
  # over from the unprivileged build job.
  sign:
    needs: build
    runs-on: ubuntu-latest
    environment: plugin-signing
    permissions:
      contents: write
    steps:
      - name: Checkout trusted main
        uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
      - name: Assert tag commit is an ancestor of main
        run: |
          git fetch --force origin "+refs/tags/$GITHUB_REF_NAME:refs/tags/$GITHUB_REF_NAME"
          tag_commit=$(git rev-parse "refs/tags/$GITHUB_REF_NAME^{commit}")
          if ! git merge-base --is-ancestor "$tag_commit" refs/remotes/origin/main; then
            echo "tag $GITHUB_REF_NAME ($tag_commit) is not an ancestor of main — refusing to sign"
            exit 1
          fi
      - name: Parse tag (trusted scripts)
        id: tag
        run: |
          json=$(node scripts/parse-tag.mjs "$GITHUB_REF_NAME")
          echo "id=$(echo "$json" | node -e 'process.stdout.write(JSON.parse(require("node:fs").readFileSync(0,"utf8")).id)')" >> "$GITHUB_OUTPUT"
          echo "version=$(echo "$json" | node -e 'process.stdout.write(JSON.parse(require("node:fs").readFileSync(0,"utf8")).version)')" >> "$GITHUB_OUTPUT"
      - name: Download unsigned artifact
        uses: actions/download-artifact@v8
        with:
          name: moext-unsigned
          path: dist/artifacts
      - name: Sanity-check artifact size cap
        env:
          PLUGIN_ID: ${{ steps.tag.outputs.id }}
          PLUGIN_VERSION: ${{ steps.tag.outputs.version }}
        run: |
          f="dist/artifacts/${PLUGIN_ID}-${PLUGIN_VERSION}.moext"
          size=$(wc -c < "$f")
          [ "$size" -le 5242880 ] || { echo "$f is ${size}B > 5 MiB cap"; exit 1; }
      - name: Sign pre-built bytes
        env:
          MOTRIX_SIGNING_KEY_PEM: ${{ secrets.MOTRIX_PLUGIN_SIGNING_KEY }}
          PLUGIN_ID: ${{ steps.tag.outputs.id }}
          PLUGIN_VERSION: ${{ steps.tag.outputs.version }}
        run: node scripts/sign.mjs "dist/artifacts/${PLUGIN_ID}-${PLUGIN_VERSION}.moext"
      - name: Cross-check signature against committed public key
        env:
          PLUGIN_ID: ${{ steps.tag.outputs.id }}
          PLUGIN_VERSION: ${{ steps.tag.outputs.version }}
        run: node scripts/verify.mjs "dist/artifacts/${PLUGIN_ID}-${PLUGIN_VERSION}.moext" --pub keys/signing-key.pub.pem
      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          PLUGIN_ID: ${{ steps.tag.outputs.id }}
          PLUGIN_VERSION: ${{ steps.tag.outputs.version }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            "dist/artifacts/${PLUGIN_ID}-${PLUGIN_VERSION}.moext" \
            "dist/artifacts/${PLUGIN_ID}-${PLUGIN_VERSION}.moext.sig" \
            "dist/artifacts/${PLUGIN_ID}-${PLUGIN_VERSION}.metadata.json" \
            --title "$GITHUB_REF_NAME" --notes "Signed .moext release for ${PLUGIN_ID} ${PLUGIN_VERSION}"
      - name: Notify plugin-registry
        # Opens a registry-entry-update PR on motrixapp/plugin-registry. The
        # empty-token shell guard (not a step-level `if: secrets`, which is not
        # reliably available) makes this a no-op until the token is provisioned,
        # so the release still succeeds; the entry can be updated manually via
        # the registry's workflow_dispatch in the meantime.
        env:
          GH_TOKEN: ${{ secrets.REGISTRY_DISPATCH_TOKEN }}
          PLUGIN_ID: ${{ steps.tag.outputs.id }}
          PLUGIN_VERSION: ${{ steps.tag.outputs.version }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "REGISTRY_DISPATCH_TOKEN not set — skipping registry notify"
            exit 0
          fi
          gh api repos/motrixapp/plugin-registry/dispatches \
            -f event_type=builtin-released \
            -F client_payload[id]="$PLUGIN_ID" \
            -F client_payload[version]="$PLUGIN_VERSION" \
            -F client_payload[tag]="$GITHUB_REF_NAME"
